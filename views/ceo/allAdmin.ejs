<div class="min-h-screen bg-gray-100 p-6">
  <div class="max-w-4xl mx-auto">
    <div>
      <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center">
        👨‍💼 Admins
      </h1>
      <button class=""><a href="/ceo/create-admin">Create new One</a></button>
    </div>
    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      <% if (admins.length === 0) { %>
      <h2 class="text-xl font-semibold text-center text-red-500 mt-10">
        🚫 No admins found!
      </h2>
      <% } else {%> <% admins.forEach(admin => { %>
      <div
        class="bg-white shadow-md rounded-2xl p-5 border border-gray-200 hover:shadow-lg transition"
      >
        <a href="/ceo/user/<%= admin.id %>">
          <h2 class="text-lg font-semibold text-gray-800"><%= admin.name %></h2>
          <p class="text-sm text-gray-600 mt-1"><%= admin.email %></p>
          <div class="flex gap-8 justify-center items-between">
            <a class="text-blue-300 cursor-pointer"> edit </a>
            <a class="text-blue-800 cursor-pointer"> delete </a>
          </div>
        </a>
      </div>

      <% }) %> <% } %>
    </div>
  </div>
</div>
