<div class="bg-white p-6 rounded-lg shadow-md">
  <h1 class="text-3xl font-bold text-gray-800 mb-6">Admin Dashboard Overview</h1>
  <p class="text-gray-600 mb-4">
    Welcome, <%= user.name %>. This is your primary dashboard where you can see
    admin-level metrics and manage your user and their tasks.
  </p>

  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8">
    <div class="bg-green-500 text-white p-4 rounded-lg shadow-lg">
      <h2 class="text-xl font-semibold">Total Users</h2>
      <% if (totalUsers) { %>
       <p class="text-3xl mt-2"><%= totalUsers %></p>
      <% } else {%>
      <p class="text-3xl mt-2">Loading...</p>
      <% } %>
    </div>
    <div class="bg-yellow-500 text-white p-4 rounded-lg shadow-lg">
      <h2 class="text-xl font-semibold">Pending Tasks</h2>
      <% if (totalTasks) { %>
        <p class="text-3xl mt-2"><%= totalTasks %></p>
      <% } else {%>
        <p class="text-3xl mt-2">Loading...</p>
      <% } %>
    </div>
  </div>
</div>